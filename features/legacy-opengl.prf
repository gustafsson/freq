# OpenGL 4 is not supported by QPainter  which is central to QWidget/QGLWidget (tested in Qt 5.5)
# So define LEGACY_OPENGL when building libraries for Sonic AWE (which is using QWidget)

# most backwards compatibility
#!macx-ios*: DEFINES += LEGACY_OPENGL

!macx*: DEFINES += LEGACY_OPENGL
